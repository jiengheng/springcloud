server:
  port: 9898

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/jiengheng/config-server.git


eureka:
  client:
    service-url:
      #      defaultZone: http://127.0.0.1:9090/eureka,http://127.0.0.1:9091/eureka
      defaultZone: http://127.0.0.1:9090/eureka
